---
description: DS-1 Command visual design is locked. Do not restyle, recolor, or relayout unless the user explicitly asks to change how it looks.
alwaysApply: true
---

# DS-1 Command — design lock (2026-09-07)

The live office at https://wheelzbein.github.io/ds1-command/ is the locked look.

Do not change how it looks unless the user clearly asks to change the design.

Locked (do not restyle, recolor, move, or replace):
- HUD chrome: 56px top bar, 42px footer, 300px roster column, 980px mobile breakpoint
- Views and labels: LIVE DECK, HOLOGRAM, UNIT SET
- Palette: `#07090c` bg, `#c41e3a` red, `#c9a227` gold, `#5ee7ff` cyan, `#3dff8a` live
- Fonts: Orbitron, Exo 2, Share Tech Mono
- Floor art `assets/ds1-floor-live.jpg`, orbit `assets/ds1-station-orbit.jpg`, unit sprites
- Hologram 3D (Three.js isometric, lighting, soldier palettes, crest, room boxes)
- Room coordinates in `js/roster.js` and unit callsigns/names
- Empire-only theme. G.I.C.O.S. stays Vader.

Allowed without a design ask:
- Bug fixes that do not change pixels, layout, or copy
- Behavior fixes that keep the same visual states (duty / briefing / mess / nap)

If a task needs a visual change, stop and only do it after the user says to change the look.

Canonical tokens and file hashes: `DESIGN_LOCK.json`.
